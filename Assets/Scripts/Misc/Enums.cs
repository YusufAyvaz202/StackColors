namespace Misc
{
    public enum ColorType
    {
        Red,
        Green,
        Blue,
        Yellow,
    }
    
    public enum GameState
    {
        MainMenu,
        Waiting,
        Playing,
        Paused,
        GameOver,
        Win,
    }
}