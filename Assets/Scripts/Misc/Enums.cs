namespace Misc
{
    public enum ColorType
    {
        Red,
        Green,
        Blue,
        Yellow,
    }
    
    public enum GameState
    {
        MainMenu,
        Playing,
        Paused,
        GameOver,
        Win,
    }
}